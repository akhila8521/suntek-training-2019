#include<stdio.h>
long long int factorial(char arr[])
{
 int n=atoi(arr),i;
 long long int fact1=1;
 for(i=1;i<=n;i++)
 {
  fact1=fact1*i;
   }
  return fact1;
}
int main()
{
char fact[10];
scanf("%s",fact);
int F=factorial(fact);
printf("%lld",F);
return 0;
}

